# Copy this file to ".env" and paste your real MongoDB Atlas connection string below.
# NEVER commit the real .env file to GitHub — it contains your password.

MONGO_URI=mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/studentPortal?retryWrites=true&w=majority
PORT=3000
